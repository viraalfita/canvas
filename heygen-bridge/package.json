{
  "name": "heygen-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker bridge between Canvas and HeyGen MCP. Handles OAuth (DCR + PKCE), token storage, async polling, and webhook fan-out.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "hono": "^4.9.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.100.0"
  }
}
